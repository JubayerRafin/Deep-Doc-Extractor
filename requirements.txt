# ────────────────────────────────────────────────────────────────────
# Deep Doc Extractor — Python dependencies
# Tested on Python 3.11 (Ubuntu 22.04, Windows 10/11)
# ────────────────────────────────────────────────────────────────────

# Core config
pyyaml>=6.0

# Stage 1 — PDF extraction
pdfplumber>=0.11.0
camelot-py[cv]>=0.11.0
Pillow>=10.0.0

# Stage 2/3/4 — LLM communication
requests>=2.31.0

# Stage 2/3/4 — Embeddings, similarity, vector store
sentence-transformers>=2.7.0
numpy>=1.24.0

# Stage 4 — RAG service
chromadb>=0.5.0
streamlit>=1.30.0

# Evaluation — NLI faithfulness check
transformers>=4.40.0
torch>=2.2.0

# Testing (optional, dev only)
pytest>=8.0.0
pytest-cov>=4.1.0
