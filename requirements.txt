# Deep Doc Extractor — Python dependencies
# Python 3.11 recommended

# ── Layer 1: extraction ──────────────────────────────────
pdfplumber>=0.11
camelot-py[cv]>=0.11
Pillow>=10.0
opencv-python>=4.8
pandas>=2.1
numpy>=1.26

# ── Layers 2 & 3: LLM-based Q&A and augmentation ────────
requests>=2.31              # Ollama HTTP client
PyYAML>=6.0                 # config.yaml
sentence-transformers>=2.7  # quality filters + retrieval embeddings

# ── Layer 4: RAG service ────────────────────────────────
chromadb>=0.5
langchain>=0.2
streamlit>=1.30

# ── Testing ─────────────────────────────────────────────
pytest>=8.0
pytest-cov>=4.1

# Deep Doc Extractor — Stage 2 Dependencies
pyyaml>=6.0
requests>=2.28
sentence-transformers>=2.2.0
numpy>=1.24


# Stage 4 — additional dependencies beyond Stages 1-3
# -------------------------------------------------------
# (chromadb 0.5.x is pinned deliberately — 0.6 changed persistence format)
chromadb>=0.5.0,<0.6.0
sentence-transformers>=2.2.0
streamlit>=1.30
numpy>=1.24
pyyaml>=6.0
requests>=2.28
